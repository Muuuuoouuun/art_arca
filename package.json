{
  "name": "art-arca",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "generate-images": "node --env-file=.env.local scripts/generate-images.mjs",
    "generate-images:thumbs": "node --env-file=.env.local scripts/generate-images.mjs --type=thumbs",
    "generate-images:heroes": "node --env-file=.env.local scripts/generate-images.mjs --type=heroes",
    "generate-images:icons": "node --env-file=.env.local scripts/generate-images.mjs --type=icons",
    "generate-images:backgrounds": "node --env-file=.env.local scripts/generate-images.mjs --type=backgrounds"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "next": "15.2.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.2.2",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
